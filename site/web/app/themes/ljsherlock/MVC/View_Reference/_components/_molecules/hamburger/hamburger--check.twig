
   {# menu toggle: place in body  #}
   {{ el.createHTMLTag( 'input', { type: 'checkbox', name: 'nav__check--hamburger', id: 'nav__check--hamburger' }, [ { block: 'hamburger__check' }], '' ) }}

   {{ el.createHTMLTag( 'div', {}, [ { block: 'overlay' }, { block: 'menu__overlay' }], '' ) }}
