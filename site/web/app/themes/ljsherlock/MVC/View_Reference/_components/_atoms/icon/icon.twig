{#
	* @desc         Create icons!
	* @name         icon
	* @author       ljsherlock
	* @tested       Twig
	* @requires     svg.twig
#}
{% if bem is defined and bem is iterable %}

   {% set tmpClasses = [{block: 'icon'}] | merge( bem )  %}

{% else %}

   {% set tmpClasses = [{block: 'icon'}]  %}

{% endif %}

{% include '_atoms/svg/svg.twig' with { bem: tmpClasses } %}
