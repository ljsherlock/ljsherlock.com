{#
	* @desc         Create Blocks!
	* @name         Block
	* @author       ljsherlock
	* @tested       Twig
	* @requires     component.twig
#}

{# merge the block into the passed elements and modifiers #}
{% if bem is defined %}

   {% set tmpClasses = [{ block: 'block' }]|merge( bem )  %}

{% else %}

   {% set tmpClasses = [{block: 'block'}]  %}

{% endif %}

{{ el.createHTMLTag('div', attr, tmpClasses, innerHTML) }}
