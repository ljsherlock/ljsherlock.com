{% import 'macros.twig' as mcro %}

{# atoms/forms/select-menu.twig #}

{% set selectHTML %}

   {% if default %}
      {{ mcro.createHTMLTag( 'option', { selected: 'selected', disabled: 'disabled', value: '0' }, selectBEM, default ) }}
   {% endif %}

   {% for option in options %}
      {{ mcro.createHTMLTag( 'option', { value: option.value }, optionBEM, option.text ) }}
   {% endfor %}

{% endset %}

{{ mcro.createHTMLTag( 'select', attrs, bem, selectHTML ) }}
