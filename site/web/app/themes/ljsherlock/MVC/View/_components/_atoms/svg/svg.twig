{#
	* @desc         Create svgs!
	* @name         SVG
	* @author       ljsherlock
	* @tested       Twig
	* @requires     component.twig, _MUSTARD
#}

{% import 'macros.twig' as mcro %}

{% set href = icon ? function('get_template_directory_uri') ~ '/MVC/View/Assets/Icons/icons.svg#' ~ icon : '' %}

{% set _use %}
   {{ mcro.createHTMLTag( 'use', { href: href, id: 'use--' ~ icon }, [], '', true ) }}
{% endset %}

{{ mcro.createHTMLTag( 'svg', {}, bem, _use ) }}
