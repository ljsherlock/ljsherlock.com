
{% import 'bem.twig' as bemify %}
{% set classname = bemify.bem(bem) %}

<input type="{{ input_type }}" class="{{ classname }}" placeholder="{{ input_placeholder }}" value="{{ input_type == 'submit' ? input_title : input_value }}" name="{{ input_name }}" id="{{ input_name }}"/>
