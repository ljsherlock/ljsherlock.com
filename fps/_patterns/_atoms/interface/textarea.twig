
{% import 'bem.twig' as bemify %}
{% set classname = bemify.bem(bem) %}

<textarea type="{{ input_type }}" class="{{ classname }}" placeholder="{{ input_placeholder }}" name="{{ input_name }}" id="{{ input_name }}">
{{input_value}}
</textarea>
