<video controls {{ autoplay ? autoplay }}>
   <source src="{{ source }}">
		<img src="{{ image }}" width="640" height="360" alt="Big Buck Bunny" title="No video playback capabilities, please download">
</video>
